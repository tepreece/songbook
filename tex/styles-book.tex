% set up fonts
\renewcommand{\lyricfont}{\normalfont\large}
\renewcommand{\notefont}{\normalfont\normalsize}

% allow songs to break over pages, unless that's a page turn
\songpos{1}

% set up index styles
\sepindexesfalse


