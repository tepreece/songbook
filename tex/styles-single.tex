% set up fonts
\renewcommand{\stitlefont}{\normalfont\Huge\textbf}
\renewcommand{\lyricfont}{\normalfont\Large}
\renewcommand{\notefont}{\normalfont\normalsize}

% allow songs to break over pages at all times
\songpos{0}

% don't show song numbers or header bars, and in one column
\nosongnumbers
\setlength{\sbarheight}{0pt}
\songcolumns{1}

% make the prelude and postlude larger
\renewcommand{\extendprelude}{
	\Large \showauthors
}

\renewcommand{\extendpostlude}{
	\normalsize \songcopyright
}

% add the attribution footnote
\pagestyle{fancy}
\lfoot{From my songbook}
\cfoot{}
\renewcommand{\headrulewidth}{0pt}







